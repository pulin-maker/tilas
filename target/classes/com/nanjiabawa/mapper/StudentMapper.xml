<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nanjiabawa.mapper.StudentMapper">
    <select id="selectByCondition" resultType="com.nanjiabawa.pojo.Student">
        select s.*, c.name as clazzName
        from student s
        left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name like concat('%', #{name}, '%')
            </if>
            <if test="degree != null">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
    </select>

    <select id="countStuclazzData" resultType="java.util.Map">
        select
            (case clazz_id
                when 1 then "数据结构"
                when 2 then "前端就业90期"
                when 3 then "JavaEE就业165期"
                when 4 then "JavaEE就业166期"
                when 5 then "大数据就业58期"
                when 6 then "JavaEE就业167期"
                else "其它" end) name,
            count(*) total from student group by clazz_id order by total desc ;
    </select>
    <select id="selectDegreeCount" resultType="java.util.Map">
        select
            (case degree
                when 1 then "初中"
                when 2 then "高中"
                when 3 then "大专"
                when 4 then "本科"
                when 5 then "硕士"
                when 6 then "博士"
                else "其它" end) degree,
        count(*) total from student group by degree order by total desc ;
    </select>

    <!--    <select id="countStuclazzData" resultType="java.util.Map">-->
<!--        select c2.name name, count(*) total from student s left join clazz c2-->
<!--            on s.clazz_id = c2.id group by c2.id order by total desc ;-->
<!--    </select>-->


    <delete id="deleteByIds">
        delete from student where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
</mapper>