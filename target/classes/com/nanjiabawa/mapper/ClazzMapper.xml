<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nanjiabawa.mapper.ClazzMapper">
    <select id="selectAll" resultType="com.nanjiabawa.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, update_time,
        case
            when curdate() &lt; begin_date then "未开班"
            when curdate() &gt; end_date then "已结课"
            else "在读中"
            end as status
        from clazz;
    </select>

    <select id="selectByCondition" resultType="com.nanjiabawa.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, update_time,
               case
                   when curdate() &lt; begin_date then "未开班"
                   when curdate() &gt; end_date then "已结课"
                   else "在读中"
                   end as status
        from clazz
        <where>
            <if test="name != null and name != ''">
                name like concat('%', #{name}, '%')
            </if>
            <if test="gender != null">
                and gender like #{gender}
            </if>
            <if test="begin != null and end != null">
                and end_date between #{begin} and #{end}
            </if>
        </where>
    </select>
</mapper>